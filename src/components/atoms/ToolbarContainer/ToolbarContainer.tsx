import React from "react";

interface IToolbatContainerProps {
  position: "left" | "right" | "bottom" | "top";
}

export const ToolbarContainer = ({ position }: IToolbatContainerProps) => {
  return <div>ToolbarContainer</div>;
};
